#include "state.h"
state_update_result_t state_apply_frame(indicator_state_t *s,
                                        const parsed_frame_t *f) {
    (void)s; (void)f;
    state_update_result_t r = {0}; return r;
}
