# Настройки воспроизведения видео на Pi.
# Не синхронизируется с MCU. Не отображается в TUI-меню.
#
# Автоматически определяется по пути --config=.

[video]
# Окно воспроизведения omxplayer в экранных пикселях: x,y,ширина,высота.
# Для дисплея 600x1024 (полный экран):
win_x = 0
win_y = 0
win_w = 600
win_h = 800